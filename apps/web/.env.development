# Non-secret dev defaults. Production (coolify) sets API_URL to the internal api
# address and NEXT_PUBLIC_API_URL to /api (traefik same-origin routing).
API_URL=http://localhost:5180
NEXT_PUBLIC_API_URL=http://localhost:5180
