// The generated contract for the Bandroom api. `pnpm gen:api` at the repo root
// re-exports the openapi document from the running code and regenerates
// schema.d.ts — api and clients cannot drift (spec §6).
export type { paths, components } from "./schema";
